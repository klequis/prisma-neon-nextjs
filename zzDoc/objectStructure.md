
```
## page
Item (key{item.id}, fullItem{item})
- {title}
- ItemDates ({itemDates})
    - {createdAt}
    - {modifiedAt}
    - {dueAt}
    - {completedAt}
- Contacts ({contacts})
    - Contact ({contact})
        - {contact.id}
        - {contact.phoneNumber}
        - {contact.phoneType}
- Notes ({notes})
    - Note ({note})
        - {createdAt}
        - {body}

```
